apiVersion: v1
kind: Service
metadata:
  name: {{ template "postal.fullname" . }}-external
spec:
  type: NodePort
  selector:
    greeting: hello
    version: one
  ports:
  - protocol: TCP
    port: 60000
    targetPort: 443